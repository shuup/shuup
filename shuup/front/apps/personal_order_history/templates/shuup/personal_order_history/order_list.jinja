{% extends "shuup/front/dashboard/dashboard.jinja" %}

{% block title -%}{% trans %}Order history{% endtrans %}{%- endblock %}
{% block content_title %}{% trans %}Order history{% endtrans %}{% endblock %}

{% block breadcrumb %}
    {% call macros.breadcrumb() %}
        {{ macros.render_breadcrumb_item("/", _("Home")) }}
        {{ macros.render_breadcrumb_item(url("shuup:dashboard"), _("Dashboard")) }}
        {{ macros.render_breadcrumb_item(None, _("Order History"), True) }}
    {% endcall %}
{% endblock %}

{% block dashboard_content %}
    {% if order_status_history %}
        <div id="order_history" class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>{{ _('Identifier') }}</th>
                        <th>{{ _('Order date') }}</th>
                        <th>{{ _('Status') }}</th>
                        <th class="text-right">{{ _('Total price') }}</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for order_status_change in order_status_history %}
                        <tr>
                            <td>{{- order_status_change.order.identifier -}}</td>
                            <td>{{ order_status_change.created_on|datetime }}</td>
                            <td>{{ order_status_change.next_order_status|string }}</td>
                            <td class="text-right">{{ order_status_change.order.taxful_total_price|money }}</td>
                            <td class="text-right">
                                <a href="{{ url("shuup:show-order", pk=order_status_change.order.pk) }}">
                                    <i class="fa fa-search"></i> {% trans %}Details{% endtrans %}
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <p class="lead">
            <i class="fa fa-info-circle text-info"></i> {{ _("You haven't made any orders yet.") }}
        </p>
    {% endif %}
{% endblock %}
